stateDiagram-v2
    [*] --> 嘗試存取訊息管理後台
    嘗試存取訊息管理後台 --> 後台身份驗證
    
    state 後台身份驗證 {
        [*] --> 檢查登入狀態
        檢查登入狀態 --> 已驗證登入 : 是
        檢查登入狀態 --> 驗證失敗 : 否
        已驗證登入 --> 進入訊息管理功能清單
        驗證失敗 --> 強制導向後台登入頁面
    }
    
    強制導向後台登入頁面 --> [*]
    
    進入訊息管理功能清單 --> 系統自資料庫撈取全站留言紀錄
    系統自資料庫撈取全站留言紀錄 --> 檢查留言狀態
    
    state 檢查留言狀態 {
        [*] --> 判斷紀錄空值
        判斷紀錄空值 --> 存有留言紀錄 : 是
        判斷紀錄空值 --> 查無任何留言 : 否
        存有留言紀錄 --> 列表呈現所有留言明細
        查無任何留言 --> 後台預設顯示目前尚無任何未讀訊息
    }
    
    後台預設顯示目前尚無任何未讀訊息 --> [*]
    
    列表呈現所有留言明細 --> 執行管理維護操作
    
    state 執行管理維護操作 {
        [*] --> 選擇操作類型
        選擇操作類型 --> 標記處理 : 點選已處理
        選擇操作類型 --> 移除留言 : 點選刪除
        標記處理 --> 更新留言狀態碼
        移除留言 --> 自資料庫刪除紀錄
    }
    
    更新留言狀態碼 --> 儲存結果判定
    自資料庫刪除紀錄 --> 儲存結果判定
    
    state 儲存結果判定 {
        [*] --> 檢查網絡與寫入
        檢查網絡與寫入 --> 寫入成功 : 是
        檢查網絡與寫入 --> 寫入失敗 : 否
        寫入成功 --> 刷新後台列表並顯示成功
        寫入失敗 --> 提示異常並保留原訊息狀態
    }
    
    刷新後台列表並顯示成功 --> [*]
    提示異常並保留原訊息狀態 --> 列表呈現所有留言明細